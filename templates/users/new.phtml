<form action="/students" method="post">
    <div>
        <label>
            Имя
            <input type="text" name="student_name" value="<?=htmlspecialchars($studentData['name']) ?? '' ?>">
        </label>
        <?php if(isset ($errors['name'])) : ?>
        <div><?=$errors['name']?></div>
        <?php endif ?>
    </div>
    <input type="submit" value="Create">

</form>